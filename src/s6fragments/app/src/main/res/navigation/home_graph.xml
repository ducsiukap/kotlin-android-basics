<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_graph"
    app:startDestination="@id/home_fragment">

    <fragment
        android:id="@+id/home_fragment"
        android:name="com.vduczz.s6fragments.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_common">

        <action
            android:id="@+id/action_home_to_detail"
            app:destination="@id/detail_fragment" />

    </fragment>

    <fragment
        android:id="@+id/detail_fragment"
        android:name="com.vduczz.s6fragments.HomeDetailFragment"
        android:label="Home"
        tools:layout="@layout/fragment_common">

        <!-- required arguments -->
        <argument
            android:name="detailId"
            app:argType="integer" />
        <!-- valid argType: integer, float, long, boolean, string, reference, package.ClassName -->

        <!-- optional / nullable fragment -->
        <!--        <argument-->
        <!--            android:name="optionalArg"-->
        <!--            android:defaultValue="HelloWorld!"-->
        <!--            app:argType="string"-->
        <!--            app:nullable="true" />-->

    </fragment>

</navigation>